server:
  port: 20020

spring:
  application:
    name: cloud-config-read
  rabbitmq:
    host: 192.168.8.131

logging:
  level:
    com:
      example:
        demo: debug

# 动态刷新配置 ---需要忽略权限拦截
management:
  endpoints:
    web:
      exposure:
        include: refresh

stuname: sakura
